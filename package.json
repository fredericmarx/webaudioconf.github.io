{
  "name": "webaudioconf.github.io",
  "description": "The website of the Web Audio Conf 2018.",
  "version": "1.0.0",
  "dependencies": {
    "font-awesome": "^4.7.0",
    "gatsby": "^1.9.127",
    "gatsby-link": "^1.6.30",
    "gatsby-plugin-less": "^1.1.6",
    "gatsby-plugin-react-helmet": "^1.0.8",
    "gatsby-plugin-sitemap": "^1.2.13",
    "gh-pages": "^1.1.0"
  },
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gatsby build && gh-pages -d public --branch master"
  },
  "devDependencies": {
    "prettier": "^1.8.2"
  },
  "private": true
}
